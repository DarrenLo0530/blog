<div class = 'form-background'>
    <h3>Post a Comment</h3>

    <%= form_for [@article, @comment] do |f| %>
        <p>
            <%= f.text_area :body %>
        </p>
        <p>
            <%= f.submit 'Submit'%>
        </p>
    <% end %>
</div>